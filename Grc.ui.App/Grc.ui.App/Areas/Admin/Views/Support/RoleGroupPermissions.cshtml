@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "GROUP PERMISSIONS";
}
@model AdminDashboardModel
@{
    var component = new PageComponentViewModel
    {
        ComponentId = "1",
        ComponentKey = "RolePermissions",
        LeftActions = new List<ComponentAction>
        {
            new ComponentAction { ActionKey = ActionKey.RoleGroupNew.GetDescription(), Icon = "mdi mdi-plus", Tooltip = ILocalize.GetLocalizedLabel("App.Label.Role.Group") },
        }
    };
}

<!-- role list -->
@await Component.InvokeAsync("BaseComponent", new { model = component, content = Component.InvokeAsync("GroupPermissionsList") })